package practice8_10.sec06;

public class text {

	
//	1. 인터페이스에서 상수를 선언할경우 접근제어자 ,static ,final 을 생략할수있다.
//	2. 인터페이스에 선언된 추상메서드는 구현클래스에서 모두 구현 해줘야하며, implements 접미어를 붙혀서 사용한다.
//	3. 여러개의 인터페이스를 구현클래스에서 구현할수있다.
//	4. 오버라이딩을통해서 각각의 구현클래스에서 구현방식을 달리하여 다형성을 구현할수있다.
//	5. 인터페이스타입 이든, 부모클래스 타입이건, 타입자리에 들어온 클래스, 인터페이스 에 선언된 메서드만 사용할수있다.
//	6. boolean 타입 매개변수는 if 문과만났을때 메서드에 true 를 넣느냐 , false 를 넣느냐에따라서 메서드 결과를 달리한다.
//	7. boolean 타입 매개변수를 사용하는 메서드가 if 문을 이용해서 구현해야한다면 else 문이 꼭 필요하다.
}
